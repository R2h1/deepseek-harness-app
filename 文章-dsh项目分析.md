# 我试着给 DeepSeek 的开源 agent 框架套了个壳，顺手把它的仓库翻了个底朝天

我最近干了一件蠢事。为了给 DeepSeek Harness 的网页界面套一个桌面壳，我在终端里起了 `dsh web`，浏览器打开 `http://127.0.0.1:3080`，看到返回 200，就以为是自己起的一个测试服务，想把它停掉。后来才反应过来，跑在 3080 上的那个进程，正是我所在的这个会话本身，我差点把自己的工作台关了。好在两次都没按下去。

这个项目值得拆开看。DeepSeek Harness 是 DeepSeek 开源的一个 agent 运行时，让模型能调工具、跑代码、管文件、执行后台任务。仓库根目录写着版本 0.1.0-rc.5，README 第一段就明说会有兼容性破坏，一副抢跑阶段的坦荡样子。但让我停下来的是它的规模和组织方式，这些东西装在一个还没发稳定版的框架里，显得很反常。

## 一切皆插件，连自己都是

它的底子是 vendored 进仓库的 Cordis 框架。所谓 vendored，就是把 Cordis 全家连同它的基础库源码直接拷进仓库，重新挂到 `@deepseek-ai` 名下，不再从 npm 拉。这样做的理由是彻底拥有框架层，可审计、可打补丁、可锁版本。光这一层，仓库里就记了十八处对上游的本地修改，从 fiber 生命周期硬化，到 HMR 的序列化，再到配置加载的事务性协调。每一处都写在 `vendor/README.md` 的修改日志里，等着下次同步上游时重新对账。

插件到什么程度。模型适配器是插件，工具注册表是插件，会话日志是插件，连 agent 主循环本身都是插件。产品里的每一块都能从配置文件替换。你要换一个模型提供商，注册一个适配器就行。你要换一套沙箱，把 provider 指过去，Bash、终端、语言服务器会跟着一起走。没有需要特批的核心，扩展 dsh 的方式就是往插件树里再挂一个插件，注销时它留下的一切效果会跟着拆掉。

## 两百多个包，按能力分成四十九组

仓库里 `packages/` 下面按能力分组，一共四十九组，两百多个包。客户端界面一个组就占了三十九个包，会话持久化十三个，子代理十一个，shell 九个。每个包有自己的 README，有自己的运行时不变量检查，覆盖率门也按文件卡着。任何一个分组里抽出一个包，都有独立的语义和扩展点，读起来像一本小型的领域模型。

这种拆法有实际的用处。它让"加一个能力"变成一个明确的动作，在哪个组、挂哪个 ctx 键、注册哪类事件，都有规矩。写一个新工具，schema 进提示词组装，执行走工具管线，界面渲染有 slot 系统管着，四层各归各。

## 网页界面其实是经典的客户端服务器结构

我原本想套壳，第一件事就是搞清楚网页界面怎么连后端。结构非常干净。浏览器那边是一个 React 单页应用，用 fetch 走 `/api` 做请求应答，用 WebSocket 走 `/mux` 和 `/host` 两个通道收事件流。后端是 Node 内置的 http 服务，监听 `127.0.0.1:3080`，它不认任何 harness 概念，只管把路由分给注册它的插件。

这个结构对套壳太友好了。壳只需要干三件事，拉一个后端进程，等端口通，再开一个窗口指向这个地址。前后端隔着标准 HTTP 和 WebSocket，壳是 Electron、Tauri 还是别的什么，后端根本不在乎。这也是我后来折腾 ElectroBun 的原因，它体积小，主进程还能直接用 TypeScript。

## 会话日志是一条硬约束

这个项目最让我服气的一条规矩，叫模型可见即记录。任何送进模型请求的内容，都必须能从会话日志里重建出来，而且有运行时断言在盯着。想给模型加一种新输入，就得先加一个新会话事件，让日志能把它还原。这条约束把"给模型看什么"和"记录什么"焊死在一起，日志因此既是历史，又是模型下一次请求的全部来源。

## 治理比代码更吓人

这个仓库最不一般的地方在治理。文档目录下子系统参考有九十二页，全部中英双语配对，每份配一个 `.i18n.yaml` 记录，还做了 Git 合并时的自动配对。决策记录一共一千多篇，按架构、过程、功能、测试、简化分类，归档的冻结，实施中的用现在时描述现状。测试覆盖率按文件卡百分之百，连工具调用和会话回放都有无密钥的录制回放套件。仓库里塞了几十个 `verify-` 开头的脚本，验证文档预算，验证导出注释，依赖图和双语配对也有对应的检查，层层都是机器能查的门。

这套东西摆在开源仓库里，最直接的效果是，AI 能在这个仓库里自己干活。文档分层的规矩、决策记录的分类、机器可查的门禁，全是为代理准备的护栏。它把"agent 怎么写代码"这件事本身制度化了。我翻到这些的时候，才理解为什么一个 0.1.0-rc 的框架要配这么重的治理，因为它的主要贡献者很可能就是模型自己。

## 套壳遇到的现实

回到我开头那件事。给这玩意套壳，技术路线其实清楚。Electron 最省事，就是大了点。Tauri 体积小，但 macOS 那头的 WebKit 跟 Chromium 有差异，而且得写 Rust。ElectroBun 夹在中间，主进程用 Bun，系统 WebView 渲染，体积十几兆，更新补丁按 KB 算，看起来是最适合的一档。

问题出在下载。ElectroBun 的核心二进制托管在 GitHub releases，五十兆的压缩包。直连超时，走镜像下到十四兆，连接被掐断，反复试都是同一个位置断。这事在仓库里没有解法，因为后端本身必须跑在 Node 上，壳用什么 runtime 都不影响它。我最终停在了这里，壳的代码已经写好，卡在下载环节，改天换网再试。

回头看这次折腾，壳最后没搭起来，倒是把这个仓库从头到尾翻了一遍。看清了一个 agent 框架要怎么把自己组织成一个 agent 能维护的代码库。两百多个包、一千多篇决策记录、机器可查的每一道门，都是为了让下一个模型走进来就能接着干活。套壳本身不难，理解它为什么长成这样，才花了我最多的时间。

写到这里，事情基本讲完了。那天我差点把自己的工作台停掉，现在想想还挺好笑，一个要给别人套壳的人，先被自己的后端困住了。
